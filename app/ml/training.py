# =============================================================================
# app/ml/training.py —— 训练数据与个性化学习
#
# 作用：
#   保存用户对标签的修正，作为个性化训练数据；支持用这些数据
#   对分类器进行增量 / 重新训练，使预测逐渐贴合用户习惯。
#
# 大致结构：
#   def save_correction(db, file_id, tag, accepted)  # 写入反馈表
#   def load_training_data(db) -> (X, y)             # 读取已确认样本
#   def retrain(classifier, X, y)                    # 个性化再训练
# =============================================================================
