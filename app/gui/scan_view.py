# =============================================================================
# app/gui/scan_view.py —— 扫描视图（工作流步骤 ①）
#
# 作用：
#   让用户选择要整理的文件或文件夹，发起递归扫描，
#   展示扫描进度与已索引文件列表。
#
# 大致结构：
#   class ScanView(QWidget)
#       _build_ui()               # 目录选择框 + “开始扫描”按钮 + 文件表格
#       select_folder()           # 打开目录选择对话框
#       start_scan()              # 调用 app.core.scanner 递归扫描
#       _update_progress(...)     # 进度信号 → 状态栏
#       files_scanned()           # 扫描完成信号（交给 MainWindow 流转）
# =============================================================================
