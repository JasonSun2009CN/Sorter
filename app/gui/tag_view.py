# =============================================================================
# app/gui/tag_view.py —— 标签审核视图（工作流步骤 ②）
#
# 作用：
#   展示每个文件的预测标签与置信度，允许用户接受 / 拒绝 / 增删改标签。
#   用户修正会存入数据库，供后续个性化训练使用。
#
# 大致结构：
#   class TagView(QWidget)
#       _build_ui()               # 文件列表 + 标签展示区
#       load_files(files)         # 从扫描结果加载文件
#       accept_tag(...)           # 接受预测标签
#       reject_tag(...)           # 拒绝预测标签
#       edit_tags(...)            # 手动增删改用户标签
#       save_feedback(...)        # 将修正写入训练反馈
# =============================================================================
