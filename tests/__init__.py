# =============================================================================
# tests —— 测试包
#
# 作用：
#   pytest 测试根目录。按 app 的模块对应组织测试文件，
#   例如 test_scanner.py / test_organizer.py / test_classifier.py。
#   核心逻辑层（app.core / app.ml）应优先覆盖单元测试。
# =============================================================================
